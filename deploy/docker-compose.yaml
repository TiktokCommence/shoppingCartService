services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: 123456
    ports:
      - 3307:3306
    volumes:
      - mysql-data:/var/lib/mysql  # 持久化 MySQL 数据
volumes:
  mysql-data: